export const processData = (data: any) => {
    // parse finished json into data readable by grid
    let tableData = [{
        property:"prop1",
        count: 10,
        status: 100
    }];


    return tableData;
}